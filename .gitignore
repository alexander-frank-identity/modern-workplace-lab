# Betriebssystem
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Laufzeit-Protokolle der Skripte (können Benutzernamen enthalten)
logs/
*.log
*.tmp

# Zugangsdaten, Token, Zertifikate: niemals committen
.env
*.token
*.secret
*-token.txt
*.pfx
*.cer
*.pem
secrets/

# Echte Mitarbeiterlisten nie hochladen, nur die Beispieldatei
*.csv
!04-jml/beispiel-mitarbeiter.csv
